services:
  archivist:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8470:8470"
    volumes:
      - ./data:/data
      - ./archive:/archive
    restart: unless-stopped
