{
  "name": "@twmd/app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "twMediaDownloader local companion app (plan 06) — M1/M2: ws ingest + SQLite library + CDN downloader. Electron shell is deferred to M3; this runs as a plain Node daemon.",
  "main": "src/main.js",
  "scripts": {
    "start": "node src/main.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "ws": "^8.18.0"
  }
}
